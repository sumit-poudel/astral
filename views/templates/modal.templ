package templates

templ Modal() {
	<div
		class="fixed inset-0 flex items-center justify-center"
		data-show="$modalOpen"
		style="display: none"
	>
		<div class="w-full max-w-md">
			<div id="modal">
				{ children... }
			</div>
		</div>
	</div>
}
